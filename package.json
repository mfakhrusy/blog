{
  "name": "blog-fahru-me",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && python3 -m http.server 8001 -d dist",
    "deploy": "npm run build && wrangler pages deploy ./dist --project-name=blog-fahru-me"
  },
  "devDependencies": {
    "wrangler": "^4"
  },
  "dependencies": {
    "marked": "^15.0.0"
  }
}
